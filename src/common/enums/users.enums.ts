// Foydalanuvchi jinsi
export enum Gender {
    male = "male",
    female = "female",
    other = "other"
}

export enum UsersRole {
    superadmin = "superadmin",
    admin = "admin",
    owner = "owner",
    client = "client",
}