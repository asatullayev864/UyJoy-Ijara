// Booking holati
export enum BookingStatus {
    requested = "requested",   // mijoz uyni so‘ragan
    accepted = "accepted",    // egasi tasdiqlagan
    declined = "declined",    // rad etilgan
    completed = "completed"    // ijara muddati tugagan
}